//In the qn,there're  given two strings s and t.

//String t is generated by random shuffling of string s and then add one more letter at a random position.

// output: to return the letter that was added to t.

class Solution {
// creating a function and  passing input two strings as arguments
    public char findTheDifference(String s, String t) { 
//initializing a character type with null character in it
       char c=0;
       //filtering of newly added character with bitwise operation 
       for(char cs : s.toCharArray())c^=cs;
       for (char ct : t.toCharArray())c^=ct;
       return c; 
    }
}
